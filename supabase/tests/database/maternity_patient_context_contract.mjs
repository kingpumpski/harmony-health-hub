import assert from 'node:assert/strict'; import fs from 'node:fs'; const s=fs.readFileSync('supabase/migrations/20260913005500_maternity_workflow_security.sql','utf8'); assert(s.includes("COALESCE(status,'active') <> 'inactive'"),'maternity creation rejects inactive patients'); assert(s.includes("JOIN public.patients p ON p.id = me.patient_id"),'maternity observation resolves patient context'); assert(s.includes("COALESCE(p.status,'active') <> 'inactive'"),'maternity observations reject inactive patients'); console.log('maternity patient-context contract: ok');