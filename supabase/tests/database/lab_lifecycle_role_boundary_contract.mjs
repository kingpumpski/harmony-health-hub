import assert from 'node:assert/strict'; import fs from 'node:fs'; const s=fs.readFileSync('supabase/migrations/20260912203000_clinical_module_workflow_security.sql','utf8'); assert(!s.includes('is_clinical_staff(auth.uid())'),'lab workflows no longer depend on revoked helper'); assert(s.includes("has_role(auth.uid(),'lab_technician')"),'lab technician role supported'); assert(s.includes('REVOKE ALL ON FUNCTION public.collect_lab_sample'),'sample collection execution hardened'); assert(s.includes('REVOKE ALL ON FUNCTION public.enter_lab_result'),'result entry execution hardened'); assert(s.includes('REVOKE ALL ON FUNCTION public.approve_lab_result'),'approval execution hardened'); console.log('lab lifecycle role boundary contract: ok');