# Production migration baseline reconciliation

This checkpoint requests the repository migration workflow to rebuild the executable migration chain from the canonical Harmony production schema. The production database remains the source of truth; no application data should be modified or rolled back.
