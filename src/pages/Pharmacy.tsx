import { useCallback, useEffect, useMemo, useState } from 'react';
import { CreditCard, Package, Pill, RefreshCw, Search, ShoppingCart } from 'lucide-react';
import { supabase } from '@/integrations/supabase/client';
import { toast } from 'sonner';

type Patient = { id: string; first_name: string; last_name: string; patient_code: string };
type InventoryItem = { id: string; drug_name: string; brand_name: string | null; generic_name: string | null; form: string | null; strength: string | null; stock_quantity: number; reorder_level: number; unit_price: number; supplier: string | null; batch_number: string | null; expiry_date: string | null };
type Prescription = { id: string; patient_id: string; medication: string; dosage: string | null; frequency: string | null; duration: string | null; computed_quantity: number | null; status: string; patients?: Patient };
type Plan = { id: string; medication_name: string; prepared_quantity: number; service_order_id: string | null; service_order_status?: string | null; patients?: Patient };
type Alternative = Pick<InventoryItem, 'id' | 'drug_name' | 'brand_name' | 'generic_name' | 'strength' | 'form' | 'supplier' | 'stock_quantity' | 'unit_price'>;
type PosSale = { id: string; medication: string; quantity: number; total_amount: number; status: string; service_order_id: string | null };
const db = supabase as any;

export default function Pharmacy() {
  const [tab, setTab] = useState<'dispense' | 'pos' | 'inventory'>('dispense');
  const [patients, setPatients] = useState<Patient[]>([]);
  const [inventory, setInventory] = useState<InventoryItem[]>([]);
  const [prescriptions, setPrescriptions] = useState<Prescription[]>([]);
  const [plans, setPlans] = useState<Plan[]>([]);
  const [posSales, setPosSales] = useState<PosSale[]>([]);
  const [alternatives, setAlternatives] = useState<Record<string, Alternative[]>>({});
  const [patientId, setPatientId] = useState('');
  const [search, setSearch] = useState('');
  const [matches, setMatches] = useState<Record<string, string>>({});
  const [quantities, setQuantities] = useState<Record<string, number>>({});
  const [posItem, setPosItem] = useState('');
  const [posPatient, setPosPatient] = useState('');
  const [posQuantity, setPosQuantity] = useState(1);
  const [loading, setLoading] = useState(false);
  const [inventoryForm, setInventoryForm] = useState({ drug_name: '', brand_name: '', generic_name: '', strength: '', form: '', supplier: '', batch_number: '', expiry_date: '', stock_quantity: 0, reorder_level: 20, unit_price: 0 });

  const load = useCallback(async () => {
    setLoading(true);
    const [{ data: patientRows }, { data: inventoryRows }, { data: prescriptionRows }, { data: planRows }, { data: posRows }] = await Promise.all([
      supabase.from('patients').select('id,first_name,last_name,patient_code').order('first_name').limit(1000),
      supabase.from('pharmacy_inventory').select('*').order('drug_name'),
      supabase.from('prescriptions').select('id,patient_id,medication,dosage,frequency,duration,computed_quantity,status,patients(id,first_name,last_name,patient_code)').in('status', ['pending', 'paid']).order('created_at', { ascending: false }).limit(200),
      supabase.from('pharmacy_dispensing_plans').select('*,patients(id,first_name,last_name,patient_code)').neq('status', 'cancelled').order('created_at', { ascending: false }).limit(200),
      supabase.from('pharmacy_pos_sales').select('id,medication,quantity,total_amount,status,service_order_id').neq('status', 'dispensed').neq('status', 'cancelled').order('created_at', { ascending: false }).limit(100),
    ]);
    setPatients((patientRows ?? []) as Patient[]); setInventory((inventoryRows ?? []) as InventoryItem[]); setPrescriptions((prescriptionRows ?? []) as Prescription[]);
    const rawPlans = (planRows ?? []) as Plan[]; const orderIds = rawPlans.map((plan) => plan.service_order_id).filter(Boolean) as string[];
    const { data: orders } = orderIds.length ? await supabase.from('service_orders').select('id,status').in('id', orderIds) : { data: [] };
    const statusMap = Object.fromEntries((orders ?? []).map((order) => [order.id, order.status])); setPlans(rawPlans.map((plan) => ({ ...plan, service_order_status: plan.service_order_id ? statusMap[plan.service_order_id] ?? null : null })));
    const rawSales = (posRows ?? []) as PosSale[]; const saleIds = rawSales.map((sale) => sale.service_order_id).filter(Boolean) as string[];
    const { data: saleOrders } = saleIds.length ? await supabase.from('service_orders').select('id,status').in('id', saleIds) : { data: [] };
    const saleStatus = Object.fromEntries((saleOrders ?? []).map((order) => [order.id, order.status])); setPosSales(rawSales.map((sale) => ({ ...sale, status: sale.service_order_id ? saleStatus[sale.service_order_id] ?? sale.status : sale.status })));
    setLoading(false);
  }, []);

  useEffect(() => { void load(); }, [load]);
  const visiblePrescriptions = useMemo(() => prescriptions.filter((item) => !patientId || item.patient_id === patientId), [patientId, prescriptions]);
  const visibleInventory = useMemo(() => inventory.filter((item) => `${item.drug_name} ${item.brand_name ?? ''} ${item.generic_name ?? ''} ${item.supplier ?? ''}`.toLowerCase().includes(search.toLowerCase())), [inventory, search]);
  const findAlternatives = async (prescription: Prescription) => { const { data, error } = await db.rpc('find_pharmacy_alternatives', { _medication: prescription.medication, _strength: prescription.dosage }); if (error) return toast.error(error.message); setAlternatives((current) => ({ ...current, [prescription.id]: (data ?? []) as Alternative[] })); };
  const prepare = async (prescription: Prescription) => { const inventoryId = matches[prescription.id]; if (!inventoryId) return toast.error('Select the available product or an approved alternative.'); const { error } = await db.rpc('prepare_pharmacy_dispensing', { _prescription_id: prescription.id, _inventory_id: inventoryId, _quantity: Math.max(1, quantities[prescription.id] ?? prescription.computed_quantity ?? 1), _notes: null }); if (error) return toast.error(error.message); toast.success('Prescription prepared. Accounts must receive payment before dispensing.'); void load(); };
  const dispense = async (plan: Plan) => { const { error } = await db.rpc('confirm_pharmacy_dispense', { _plan_id: plan.id }); if (error) return toast.error(error.message); toast.success(`${plan.medication_name} dispensed.`); void load(); };
  const createPosSale = async (event: React.FormEvent) => { event.preventDefault(); if (!posItem || posQuantity < 1) return toast.error('Select a product and quantity.'); const { error } = await db.rpc('create_pharmacy_pos_sale', { _patient_id: posPatient || null, _inventory_id: posItem, _quantity: posQuantity }); if (error) return toast.error(error.message); toast.success('Walk-in sale created and sent for payment release.'); setPosItem(''); setPosPatient(''); setPosQuantity(1); void load(); };
  const addInventory = async (event: React.FormEvent) => { event.preventDefault(); if (!inventoryForm.drug_name.trim()) return toast.error('Drug name is required.'); const { error } = await db.rpc('create_pharmacy_inventory_item', { _drug_name: inventoryForm.drug_name, _brand_name: inventoryForm.brand_name || null, _generic_name: inventoryForm.generic_name || null, _strength: inventoryForm.strength || null, _form: inventoryForm.form || null, _supplier: inventoryForm.supplier || null, _batch_number: inventoryForm.batch_number || null, _expiry_date: inventoryForm.expiry_date || null, _stock_quantity: inventoryForm.stock_quantity, _reorder_level: inventoryForm.reorder_level, _unit_price: inventoryForm.unit_price }); if (error) return toast.error(error.message); toast.success('Product added to the pharmacy store.'); setInventoryForm({ drug_name: '', brand_name: '', generic_name: '', strength: '', form: '', supplier: '', batch_number: '', expiry_date: '', stock_quantity: 0, reorder_level: 20, unit_price: 0 }); void load(); };

  return <div className="space-y-6 animate-fade-in"><header className="flex flex-col gap-3 sm:flex-row sm:justify-between sm:items-center"><div><h1 className="text-2xl font-heading font-bold flex items-center gap-2"><Pill className="w-6 h-6 text-primary" /> Pharmacy & Dispensing</h1><p className="text-muted-foreground">Prepare, pay, release and dispense with stock alternatives when the prescribed product is unavailable.</p></div><button onClick={() => void load()} className="btn-secondary inline-flex items-center gap-2"><RefreshCw className="w-4 h-4" /> {loading ? 'Refreshing...' : 'Refresh'}</button></header><div className="rounded-xl border border-primary/20 bg-primary/5 p-3 flex gap-2 text-sm"><CreditCard className="w-4 h-4 text-primary mt-0.5" /><p className="text-muted-foreground">Preparation never reduces stock. Stock is committed only after Accounts releases the service order and the pharmacist confirms dispensing.</p></div><div className="flex flex-wrap gap-2 border-b border-border">{(['dispense', 'pos', 'inventory'] as const).map((value) => <button key={value} onClick={() => setTab(value)} className={`px-4 py-2 border-b-2 text-sm ${tab === value ? 'border-primary text-primary' : 'border-transparent text-muted-foreground'}`}>{value === 'pos' ? 'Walk-in POS' : value === 'inventory' ? 'Pharmacy store' : 'Prescription dispensing'}</button>)}</div>
    {tab === 'dispense' && <div className="space-y-6"><section className="card-medical p-5"><label className="text-sm space-y-1 block max-w-xl"><span className="font-medium">Filter by patient</span><select value={patientId} onChange={(event) => setPatientId(event.target.value)} className="input-medical w-full"><option value="">All active prescriptions</option>{patients.map((patient) => <option key={patient.id} value={patient.id}>{patient.first_name} {patient.last_name} · {patient.patient_code}</option>)}</select></label></section>{visiblePrescriptions.map((prescription) => { const choices = alternatives[prescription.id] ?? []; return <section key={prescription.id} className="card-medical p-5 space-y-3"><div className="flex flex-wrap justify-between gap-3"><div><h2 className="font-semibold">{prescription.medication}</h2><p className="text-sm text-muted-foreground">{prescription.patients ? `${prescription.patients.first_name} ${prescription.patients.last_name}` : 'Patient'} · {prescription.dosage ?? 'Dose not specified'} · {prescription.frequency ?? 'Frequency not specified'} · {prescription.status}</p></div><button type="button" onClick={() => void findAlternatives(prescription)} className="btn-secondary text-sm">Find alternatives</button></div><div className="grid gap-3 md:grid-cols-[1fr_120px_auto]"><select value={matches[prescription.id] ?? ''} onChange={(event) => setMatches((current) => ({ ...current, [prescription.id]: event.target.value }))} className="input-medical"><option value="">Select product / supplier</option>{choices.map((item) => <option key={item.id} value={item.id}>{item.drug_name} · {item.supplier ?? 'In store'} · {item.stock_quantity} in stock</option>)}{inventory.filter((item) => item.stock_quantity > 0 && item.drug_name.toLowerCase().includes(prescription.medication.toLowerCase())).map((item) => <option key={item.id} value={item.id}>{item.drug_name} · {item.supplier ?? 'In store'} · {item.stock_quantity} in stock</option>)}</select><input type="number" min="1" value={quantities[prescription.id] ?? prescription.computed_quantity ?? 1} onChange={(event) => setQuantities((current) => ({ ...current, [prescription.id]: Number(event.target.value) }))} className="input-medical" /><button type="button" onClick={() => void prepare(prescription)} className="btn-primary">Prepare</button></div>{choices.length > 0 && <p className="text-xs text-success">Available alternatives are shown by brand, supplier and current stock.</p>}</section>; })}<section className="space-y-3"><h2 className="font-semibold">Prepared orders awaiting release or dispensing</h2>{plans.map((plan) => <div key={plan.id} className="card-medical p-4 flex flex-wrap items-center justify-between gap-3"><div><p className="font-medium">{plan.medication_name} · {plan.prepared_quantity}</p><p className="text-xs text-muted-foreground">{plan.patients ? `${plan.patients.first_name} ${plan.patients.last_name}` : 'Patient'} · {plan.service_order_status ?? 'awaiting release'}</p></div><button type="button" disabled={!['released', 'in_progress'].includes(plan.service_order_status ?? '')} onClick={() => void dispense(plan)} className="btn-primary disabled:opacity-50">Dispense</button></div>)}</section></div>}
    {tab === 'pos' && <div className="grid gap-6 lg:grid-cols-2"><form onSubmit={createPosSale} className="card-medical p-5 space-y-4"><h2 className="font-semibold flex items-center gap-2"><ShoppingCart className="w-4 h-4" /> Walk-in prescription sale</h2><select value={posPatient} onChange={(event) => setPosPatient(event.target.value)} className="input-medical w-full"><option value="">Walk-in / no patient record</option>{patients.map((patient) => <option key={patient.id} value={patient.id}>{patient.first_name} {patient.last_name} · {patient.patient_code}</option>)}</select><select value={posItem} onChange={(event) => setPosItem(event.target.value)} className="input-medical w-full"><option value="">Select in-stock medication</option>{inventory.filter((item) => item.stock_quantity > 0).map((item) => <option key={item.id} value={item.id}>{item.drug_name} · {item.stock_quantity} in stock · GHS {item.unit_price}</option>)}</select><input type="number" min="1" value={posQuantity} onChange={(event) => setPosQuantity(Number(event.target.value))} className="input-medical w-full" /><button className="btn-primary w-full">Create payment order</button></form><section className="card-medical p-5 space-y-3"><h2 className="font-semibold">POS release queue</h2>{posSales.map((sale) => <div key={sale.id} className="border rounded-xl p-3 flex items-center justify-between gap-3"><div><p className="font-medium">{sale.medication} · {sale.quantity}</p><p className="text-xs text-muted-foreground">GHS {sale.total_amount} · {sale.status}</p></div><button type="button" disabled={!['released', 'in_progress'].includes(sale.status)} onClick={async () => { const { error } = await db.rpc('confirm_pharmacy_pos_sale', { _sale_id: sale.id }); if (error) return toast.error(error.message); toast.success('Walk-in medication dispensed.'); void load(); }} className="btn-primary disabled:opacity-50">Dispense</button></div>)}</section></div>}
    {tab === 'inventory' && <div className="grid gap-6 lg:grid-cols-[380px_1fr]"><form onSubmit={addInventory} className="card-medical p-5 space-y-3"><h2 className="font-semibold flex items-center gap-2"><Package className="w-4 h-4" /> Add store item</h2>{(['drug_name', 'brand_name', 'generic_name', 'strength', 'form', 'supplier', 'batch_number', 'expiry_date'] as const).map((field) => <input key={field} value={inventoryForm[field]} onChange={(event) => setInventoryForm((current) => ({ ...current, [field]: event.target.value }))} type={field === 'expiry_date' ? 'date' : 'text'} placeholder={field.replace('_', ' ')} className="input-medical w-full" />)}<input type="number" min="0" value={inventoryForm.stock_quantity} onChange={(event) => setInventoryForm((current) => ({ ...current, stock_quantity: Number(event.target.value) }))} placeholder="stock quantity" className="input-medical w-full" /><input type="number" min="0" value={inventoryForm.reorder_level} onChange={(event) => setInventoryForm((current) => ({ ...current, reorder_level: Number(event.target.value) }))} placeholder="reorder level" className="input-medical w-full" /><input type="number" min="0" step="0.01" value={inventoryForm.unit_price} onChange={(event) => setInventoryForm((current) => ({ ...current, unit_price: Number(event.target.value) }))} placeholder="unit price" className="input-medical w-full" /><button className="btn-primary w-full">Add to store</button></form><section className="card-medical p-5 space-y-3"><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search product, brand or supplier" className="input-medical pl-9 w-full" /></div>{visibleInventory.map((item) => <div key={item.id} className={`border rounded-xl p-3 flex flex-wrap justify-between gap-3 ${item.stock_quantity <= item.reorder_level ? 'border-warning/60 bg-warning/5' : ''}`}><div><p className="font-medium">{item.drug_name}{item.brand_name ? ` · ${item.brand_name}` : ''}</p><p className="text-xs text-muted-foreground">{item.generic_name ?? 'Generic not recorded'} · {item.strength ?? 'Strength not recorded'} · {item.supplier ?? 'Supplier not recorded'}</p></div><div className="text-right text-sm"><p>{item.stock_quantity} in stock</p><p className="text-xs text-muted-foreground">GHS {item.unit_price} · reorder {item.reorder_level}</p></div></div>)}</section></div>}
  </div>;
}
