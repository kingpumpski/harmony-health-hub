import { useEffect, useMemo, useState } from 'react';
import { AlertTriangle, HeartPulse, ListChecks, ShieldAlert } from 'lucide-react';
import { supabase } from '@/integrations/supabase/client';
import { useAuth } from '@/contexts/AuthContext';
import { toast } from '@/hooks/use-toast';
import { evaluateTriagePriority } from '@/lib/healthApi';
import { queueOfflineTriageAssessment } from '@/lib/offlineTriage';
import { notifyRoles } from '@/lib/notifications';
import { playWorkflowSound } from '@/lib/workflowFeedback';

type Priority = 'Critical' | 'Urgent' | 'Moderate' | 'Routine';
interface Patient { id: string; patient_code: string | null; first_name: string; last_name: string }
interface TriageRow { id: string; patient_id: string; priority: string; systolic: number; diastolic: number; heart_rate: number; temperature: number; oxygen_saturation: number; weight_kg: number | null; height_m: number | null; bmi: number | null; created_at: string; patients?: { first_name: string; last_name: string } | null }
const NORMAL = { systolic: { label: 'Systolic BP', unit: 'mmHg', low: 90, high: 120 }, diastolic: { label: 'Diastolic BP', unit: 'mmHg', low: 60, high: 80 }, heartRate: { label: 'Heart rate', unit: 'bpm', low: 60, high: 100 }, temperature: { label: 'Temperature', unit: '°C', low: 36.1, high: 37.2 }, respiratoryRate: { label: 'Respiratory rate', unit: '/min', low: 12, high: 20 }, oxygenSaturation: { label: 'Oxygen saturation', unit: '%', low: 95, high: 100 } };
const priorityLabel = (value: string): Priority => { const n = value.toLowerCase(); if (n === 'critical') return 'Critical'; if (n === 'urgent') return 'Urgent'; if (n === 'moderate') return 'Moderate'; return 'Routine'; };
const bmiCategory = (value: number) => { if (!value || value <= 0) return 'Unavailable'; if (value < 18.5) return 'Underweight'; if (value < 25) return 'Healthy range'; if (value < 30) return 'Overweight'; return 'Obesity range'; };
const numeric = (value: string) => value === '' ? null : Number(value);

export default function Triage() {
  const { user } = useAuth();
  const [patients, setPatients] = useState<Patient[]>([]); const [history, setHistory] = useState<TriageRow[]>([]);
  const [patientId, setPatientId] = useState(''); const [missingPromptOpen, setMissingPromptOpen] = useState(false); const [systolic, setSystolic] = useState(''); const [diastolic, setDiastolic] = useState(''); const [heartRate, setHeartRate] = useState(''); const [temperature, setTemperature] = useState(''); const [respiratoryRate, setRespiratoryRate] = useState(''); const [oxygenSaturation, setOxygenSaturation] = useState('');
  const [weight, setWeight] = useState(''); const [height, setHeight] = useState(''); const [pain, setPain] = useState('0'); const [consciousness, setConsciousness] = useState('Alert'); const [complaint, setComplaint] = useState(''); const [notes, setNotes] = useState(''); const [priority, setPriority] = useState<Priority>('Routine'); const [saving, setSaving] = useState(false); const [offlineQueued, setOfflineQueued] = useState(false);
  const values = useMemo(() => ({ systolic: numeric(systolic), diastolic: numeric(diastolic), heartRate: numeric(heartRate), temperature: numeric(temperature), respiratoryRate: numeric(respiratoryRate), oxygenSaturation: numeric(oxygenSaturation) }), [systolic, diastolic, heartRate, temperature, respiratoryRate, oxygenSaturation]);
  const alerts = useMemo(() => Object.entries(values).flatMap(([key, value]) => { if (value === null || !NORMAL[key as keyof typeof NORMAL]) return []; const range = NORMAL[key as keyof typeof NORMAL]; if (value > range.high) return [`${range.label} ${value} ${range.unit} is above the normal upper limit of ${range.high} ${range.unit}.`]; if (value < range.low) return [`${range.label} ${value} ${range.unit} is below the normal lower limit of ${range.low} ${range.unit}.`]; return []; }), [values]);
  const load = async () => { const [{ data: pts }, { data: rows }] = await Promise.all([supabase.from('patients').select('id, patient_code, first_name, last_name').order('created_at', { ascending: false }).limit(300), supabase.from('triage_assessments' as never).select('id, patient_id, priority, systolic, diastolic, heart_rate, temperature, oxygen_saturation, weight_kg, height_m, bmi, created_at, patients(first_name,last_name)').order('created_at', { ascending: false }).limit(50)]); setPatients((pts ?? []) as Patient[]); setHistory((rows ?? []) as unknown as TriageRow[]); };
  useEffect(() => { void load(); }, []);
  useEffect(() => { if (alerts.length) toast({ title: 'Immediate clinical attention required', description: alerts.join(' '), variant: 'destructive' }); }, [alerts]);
  const missingVitalLabels = useMemo(() => [
    ['systolic', values.systolic, 'SBP'],
    ['diastolic', values.diastolic, 'DBP'],
    ['heartRate', values.heartRate, 'heart rate'],
    ['temperature', values.temperature, 'temperature'],
    ['respiratoryRate', values.respiratoryRate, 'respiratory rate'],
    ['oxygenSaturation', values.oxygenSaturation, 'SpO₂'],
  ].filter(([, value]) => value === null).map(([, , label]) => label as string), [values]);
  const missingAnthropometricLabels = useMemo(() => [
    [numeric(weight), 'weight'],
    [numeric(height), 'height'],
  ].filter(([value]) => value === null).map(([, label]) => label as string), [weight, height]);
  const bmi = useMemo(() => { const w = numeric(weight); const h = numeric(height); return w && h && w > 0 && h > 0 ? Number((w / (h * h)).toFixed(2)) : null; }, [weight, height]);
  const evaluate = () => {
    if (!missingVitalLabels.length) {
      setPriority(evaluateTriagePriority({ id: 'preview', patientId, recordedBy: user?.id ?? '', recordedAt: new Date().toISOString(), bloodPressure: { systolic: values.systolic!, diastolic: values.diastolic! }, heartRate: values.heartRate!, temperature: values.temperature!, respiratoryRate: values.respiratoryRate!, oxygenSaturation: values.oxygenSaturation!, weight: numeric(weight) ?? 0, height: numeric(height) ?? 0, notes, isCritical: alerts.length > 0 }) as Priority);
    }
    const details = missingVitalLabels.length ? 'Priority evaluation is partial because ' + missingVitalLabels.join(', ') + ' is missing. ' : '';
    toast({ title: 'Triage evaluated', description: details + (bmi ? 'Calculated BMI: ' + bmi + ' kg/m².' : 'BMI cannot be calculated until both weight and height are entered.') });
  };
  const save = async (event: React.FormEvent, allowMissing = false) => {
    event.preventDefault();
    if (!patientId || !user?.id) return toast({ title: 'Select a patient', variant: 'destructive' });
    const painValue = numeric(pain);
    if (painValue !== null && (!Number.isFinite(painValue) || painValue < 0 || painValue > 10)) return toast({ title: 'Invalid pain score', description: 'Pain score must be between 0 and 10.', variant: 'destructive' });
    if (missingVitalLabels.length + missingAnthropometricLabels.length > 0 && !allowMissing) {
      setMissingPromptOpen(true);
      return;
    }
    setSaving(true); setOfflineQueued(false); const normalized = priority.toLowerCase(); const isCritical = normalized === 'critical' || alerts.length > 0;
    const row = { id: crypto.randomUUID(), patient_id: patientId, recorded_by: user.id, systolic: values.systolic, diastolic: values.diastolic, heart_rate: values.heartRate, temperature: values.temperature, respiratory_rate: values.respiratoryRate, oxygen_saturation: values.oxygenSaturation, weight_kg: numeric(weight), height_m: numeric(height), pain_score: painValue ?? 0, consciousness, presenting_complaint: complaint || null, clinical_notes: notes || null, priority: normalized, is_critical: isCritical };
    if (!navigator.onLine) { try { await queueOfflineTriageAssessment(row); setSaving(false); setOfflineQueued(true); playWorkflowSound(isCritical ? 'critical' : 'success'); toast({ title: 'Triage saved offline', description: `${priority} assessment queued for synchronization.` }); return; } catch (error) { setSaving(false); return toast({ title: 'Offline triage unavailable', description: error instanceof Error ? error.message : 'Unable to queue this assessment.', variant: 'destructive' }); } }
    const { error } = await supabase.rpc('record_triage_assessment' as never, { _patient_id: patientId, _systolic: values.systolic, _diastolic: values.diastolic, _heart_rate: values.heartRate, _temperature: values.temperature, _respiratory_rate: values.respiratoryRate, _oxygen_saturation: values.oxygenSaturation, _weight_kg: numeric(weight), _height_m: numeric(height), _pain_score: Number(pain), _consciousness: consciousness, _presenting_complaint: complaint || null, _clinical_notes: notes || null, _priority: normalized, _is_critical: isCritical } as never);
    setSaving(false); if (error) return toast({ title: 'Triage save failed', description: error.message, variant: 'destructive' });
    playWorkflowSound(isCritical ? 'critical' : 'success');
    if (isCritical || normalized === 'urgent') await notifyRoles(['admin', 'practitioner', 'nurse', 'midwife', 'specialist_nurse'], { title: isCritical ? 'Critical vital-sign alert' : 'Urgent triage alert', message: `${priority} triage requires attention. ${alerts.join(' ') || 'Review the recorded vital signs.'}`, severity: isCritical ? 'critical' : 'warning', category: 'triage', link: '/vitals', relatedPatientId: patientId, metadata: { priority: normalized, alerts } });
    toast({ title: 'Triage recorded', description: bmi ? 'Saved with BMI ' + bmi + '.' : 'Saved; BMI is not available until both weight and height are entered.' }); setMissingPromptOpen(false); setPatientId(''); setComplaint(''); setNotes(''); setPain('0'); setSystolic(''); setDiastolic(''); setHeartRate(''); setTemperature(''); setRespiratoryRate(''); setOxygenSaturation(''); setWeight(''); setHeight(''); setPriority('Routine'); void load();
  };
  const field = (label: string, value: string, setter: (value: string) => void, placeholder: string, step = '1') => <label className="text-xs">{label}<input type="number" step={step} value={value} onChange={(e) => setter(e.target.value)} placeholder={placeholder} className="input-medical mt-1 w-full" /></label>;
  return <div className="space-y-6 animate-fade-in"><div><h1 className="text-2xl font-heading font-bold flex items-center gap-2"><HeartPulse className="w-6 h-6 text-primary" /> Triage & Vital Signs</h1><p className="text-muted-foreground">Enter measured values only. Placeholders show the expected normal range; abnormal readings are surfaced immediately for clinical attention.</p></div>{offlineQueued && <div className="rounded-xl border border-primary/20 bg-primary/5 p-3 text-sm">This assessment is saved locally and queued for synchronization. It is not yet server-confirmed.</div>}{alerts.length > 0 && <div role="alert" className="rounded-xl border-2 border-critical/60 bg-critical/10 p-4 flex gap-3"><ShieldAlert className="w-6 h-6 text-critical shrink-0" /><div><strong className="text-critical">Immediate clinical attention required</strong><ul className="text-sm mt-1 list-disc pl-5">{alerts.map((alert) => <li key={alert}>{alert}</li>)}</ul><p className="text-xs mt-2 text-muted-foreground">Recheck the measurement and follow the facility's escalation protocol.</p></div></div>}<form onSubmit={(event) => void save(event)} className="card-medical p-5 space-y-5"><div className="grid gap-3 md:grid-cols-2 lg:grid-cols-3"><select value={patientId} onChange={(e) => setPatientId(e.target.value)} className="input-medical" required><option value="">Select patient…</option>{patients.map(p => <option key={p.id} value={p.id}>{p.first_name} {p.last_name} · {p.patient_code ?? 'No code'}</option>)}</select><input value={complaint} onChange={e => setComplaint(e.target.value)} placeholder="Presenting complaint" className="input-medical" /><select value={consciousness} onChange={e => setConsciousness(e.target.value)} className="input-medical"><option>Alert</option><option>Confused</option><option>Drowsy</option><option>Unresponsive</option></select></div><div className="grid grid-cols-2 md:grid-cols-3 gap-3">{field('SBP', systolic, setSystolic, '90–120 mmHg')} {field('DBP', diastolic, setDiastolic, '60–80 mmHg')} {field('Heart rate', heartRate, setHeartRate, '60–100 bpm')} {field('Temperature °C', temperature, setTemperature, '36.1–37.2 °C', '0.1')} {field('Respiratory rate', respiratoryRate, setRespiratoryRate, '12–20 /min')} {field('SpO₂ %', oxygenSaturation, setOxygenSaturation, '95–100 %')}</div><div className="grid grid-cols-2 md:grid-cols-4 gap-3">{field('Weight kg', weight, setWeight, 'Measured weight', '0.1')} {field('Height m', height, setHeight, 'Measured height', '0.01')}<label className="text-xs">Pain score (0–10)<input type="number" min="0" max="10" value={pain} onChange={e => setPain(e.target.value)} className="input-medical mt-1 w-full" /></label><div className="rounded-xl border border-primary/20 bg-primary/5 p-3 text-sm"><span className="text-muted-foreground">Calculated BMI</span><strong className="block text-xl">{bmi ?? '—'}</strong>{bmi > 0 && <span className="text-xs text-muted-foreground">{bmiCategory(bmi)} · kg/m²</span>}</div></div><div className="grid gap-3 md:grid-cols-[1fr_1fr_2fr]"><div className="rounded-xl border border-primary/20 bg-primary/5 p-3"><span className="text-xs text-muted-foreground">Normal reference</span><p className="text-xs mt-1">SBP 90–120 · DBP 60–80 · HR 60–100 · Temp 36.1–37.2°C · RR 12–20 · SpO₂ 95–100%</p></div><div className="rounded-xl border border-primary/20 bg-primary/5 px-4 py-3"><span className="text-xs text-muted-foreground">Decision-support priority</span><strong className="block text-lg">{priority}</strong></div><textarea value={notes} onChange={e => setNotes(e.target.value)} rows={2} placeholder="Clinical notes" className="input-medical w-full" /></div>{missingPromptOpen && <div role="alertdialog" aria-label="Missing triage values" className="rounded-xl border-2 border-warning/50 bg-warning/10 p-4 space-y-3"><strong>Some values are missing</strong><p className="text-sm text-muted-foreground">Missing {missingVitalLabels.concat(missingAnthropometricLabels).join(', ')}. These fields can remain blank and the triage can still be saved, but the assessment will be incomplete.</p><div className="flex flex-wrap gap-2"><button type="button" onClick={() => setMissingPromptOpen(false)} className="btn-ghost">Review values</button><button type="button" onClick={() => { const syntheticEvent = { preventDefault: () => undefined } as React.FormEvent; void save(syntheticEvent, true); }} className="btn-primary">Save with missing values</button></div></div>}<div className="flex justify-end gap-2"><button type="button" onClick={evaluate} className="btn-ghost">Evaluate</button><button disabled={saving} className="btn-primary">{saving ? 'Saving…' : 'Save triage'}</button></div></form><div className="card-medical p-5"><div className="flex items-center gap-2 mb-4"><ListChecks className="w-5 h-5 text-primary" /><h2 className="font-semibold">Recent triage assessments</h2></div><div className="space-y-2">{history.map(row => <div key={row.id} className="rounded-xl border border-border p-3 flex flex-wrap justify-between gap-2 text-sm"><span><strong>{row.patients?.first_name} {row.patients?.last_name}</strong> · BP {row.systolic}/{row.diastolic} · SpO₂ {row.oxygen_saturation}% {row.bmi ? `· BMI ${row.bmi}` : ''}</span><span className={row.priority === 'critical' ? 'text-critical font-semibold' : row.priority === 'urgent' ? 'text-warning font-semibold' : 'text-muted-foreground'}>{priorityLabel(row.priority)} · {new Date(row.created_at).toLocaleString()}</span></div>)}{history.length === 0 && <p className="text-sm text-muted-foreground">No saved triage assessments yet.</p>}</div></div><div className="rounded-xl border border-warning/20 bg-warning/5 p-3 text-xs text-muted-foreground flex gap-2"><AlertTriangle className="w-4 h-4 text-warning shrink-0" /> Normal ranges are reference ranges for adult clinical screening and do not replace clinician interpretation, age-specific ranges or facility escalation protocols.</div></div>;
}
