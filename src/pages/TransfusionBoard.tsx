import { useEffect,useState } from 'react';
import { Droplets,RefreshCw,AlertTriangle } from 'lucide-react';
import { supabase } from '@/integrations/supabase/client';
import { toast } from '@/hooks/use-toast';
type Row={id:string;patient_id:string;blood_product:string;unit_identifier:string;blood_group:string|null;status:string;reaction_observed:boolean;reaction_notes:string|null}; type Patient={id:string;patient_code:string;first_name:string;last_name:string};
const statuses=['planned','issued','running','completed','stopped','cancelled'];
export default function TransfusionBoard(){const[rows,setRows]=useState<Row[]>([]);const[patients,setPatients]=useState<Patient[]>([]);const[busy,setBusy]=useState(false);const load=async()=>{const[p,r]=await Promise.all([supabase.from('patients').select('id,patient_code,first_name,last_name').limit(500),supabase.from('transfusion_records').select('id,patient_id,blood_product,unit_identifier,blood_group,status,reaction_observed,reaction_notes').order('created_at',{ascending:false}).limit(150)]);if(p.error||r.error)toast({title:'Unable to load transfusions',description:(p.error||r.error)?.message,variant:'destructive'});setPatients((p.data??[])as Patient[]);setRows((r.data??[])as Row[])};useEffect(()=>{void load()},[]);const transition=async(id:string,status:string)=>{setBusy(true);const{error}=await supabase.rpc('record_transfusion_event',{_record_id:id,_status:status,_reaction_observed:false,_reaction_notes:null}as never);setBusy(false);if(error)toast({title:'Transition failed',description:error.message,variant:'destructive'});else{toast({title:'Transfusion status updated'});void load()}};const reaction=async(id:string)=>{const notes=window.prompt('Document the observed reaction. Leave blank only if clinically appropriate.');if(notes===null)return;setBusy(true);const{error}=await supabase.rpc('record_transfusion_reaction',{_record_id:id,_reaction_observed:true,_reaction_notes:notes||null}as never);setBusy(false);if(error)toast({title:'Reaction update failed',description:error.message,variant:'destructive'});else{toast({title:'Reaction documented'});void load()}};const patient=(id:string)=>{const p=patients.find(x=>x.id===id);return p?`${p.patient_code} — ${p.first_name} ${p.last_name}`:'Patient'};return <div className="space-y-6"><header className="flex flex-wrap justify-between gap-3"><div><h1 className="text-2xl font-heading font-bold flex items-center gap-2"><Droplets className="h-6 w-6"/>Transfusion Board</h1><p className="text-sm text-muted-foreground">Documented transfusion lifecycle and reaction reporting. Compatibility is never inferred.</p></div><button onClick={()=>void load()} className="rounded-md border p-2"><RefreshCw className="h-4 w-4"/></button></header><div className="grid gap-3">{rows.map(r=><article key={r.id} className="rounded-xl border bg-card p-4"><div className="flex flex-wrap justify-between gap-3"><div><h2 className="font-semibold">{patient(r.patient_id)}</h2><p className="text-sm">{r.blood_product} · unit {r.unit_identifier}</p><p className="text-xs text-muted-foreground">Blood group: {r.blood_group||'Not recorded'} · {r.status}</p></div><div className="flex flex-wrap gap-2"><select disabled={busy||['completed','cancelled'].includes(r.status)} value="" onChange={e=>{if(e.target.value)void transition(r.id,e.target.value)}} className="rounded-md border bg-background px-2 py-1 text-sm"><option value="">Change status…</option>{statuses.filter(s=>s!==r.status).map(s=><option key={s}>{s}</option>)}</select><button disabled={busy} onClick={()=>void reaction(r.id)} className="rounded-md border px-2 py-1 text-sm inline-flex items-center gap-1"><AlertTriangle className="h-3 w-3"/>Reaction</button></div></div>{r.reaction_observed&&<p className="mt-2 text-sm text-destructive">Reaction documented: {r.reaction_notes||'Details not recorded'}</p>}</article>)}{!rows.length&&<p className="text-sm text-muted-foreground">No transfusion records found.</p>}</div></div>}
