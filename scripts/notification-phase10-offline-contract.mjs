import fs from 'node:fs';
import path from 'node:path';

const root=process.cwd();
const read=p=>fs.readFileSync(path.join(root,p),'utf8');
const assert=(name,ok,detail)=>{if(!ok)throw new Error('[notification-phase10] '+name+': '+detail);console.log('PASS '+name)};
const worker=read('supabase/functions/notify-queue-drain/index.ts');
const provider=read('supabase/functions/notification-provider-config/index.ts');
const webhook=read('supabase/functions/notification-webhook/index.ts');
const scheduler=read('supabase/functions/notification-scheduler/index.ts');
const send=read('supabase/functions/notifications-send/index.ts');
const settings=read('src/pages/admin/Settings.tsx');
const docs=read('docs/NOTIFICATION_PROVIDER_TEST_SETUP.md');
assert('provider-authentication',provider.includes('auth.getUser')&&provider.includes('Authorization'),'provider endpoint must require bearer authentication');
assert('provider-role-boundary',provider.includes("['admin','it_admin']")&&provider.includes('has_facility_access'),'provider configuration must require Admin/IT Admin and facility access');
assert('credential-encryption',provider.includes('NOTIFICATION_CREDENTIAL_ENCRYPTION_KEY')&&provider.includes('AES-GCM')&&provider.includes('kb.length!==32'),'provider credentials must use a 32-byte AES-GCM key');
assert('smtp-hosted-boundary',/smtpPort\s*===\s*25/.test(provider)&&/smtpPort\s*===\s*587/.test(provider)&&/credentials\.port\?\?465/.test(provider)&&/secure\s*!==\s*true/.test(provider),'hosted SMTP ports 25/587 must be rejected and 465 must require TLS');
assert('provider-routing',provider.includes('priority')&&provider.includes('isPrimary')&&provider.includes('is_primary'),'provider priority and primary routing must be persisted');
assert('worker-facility-gate',worker.includes('facility_notification_config')&&worker.includes('kill_switch')&&worker.includes('enabled_channels?.[channel]!==true'),'worker must enforce facility onboarding controls');
assert('worker-rollout',worker.includes('rolloutAllows'),'worker must enforce deterministic rollout');
assert('worker-retry',worker.includes('[10,30,120,600,3600]'),'worker retry schedule must remain explicit');
assert('worker-circuit-breaker',worker.includes('notification_provider_health')&&worker.includes('consecutive_failures'),'worker must maintain durable provider health state');
assert('scheduler-boundary',scheduler.includes('enqueue_notification_v2')&&scheduler.includes('_facility_id:row.facility_id??null'),'scheduler must preserve facility scope');
assert('send-async-boundary',send.includes('202')&&send.includes('enqueue_notification_v2'),'send API must enqueue asynchronously');
assert('webhook-idempotency',webhook.includes('notification_webhook_events'),'provider webhooks must have durable idempotency');
assert('settings-routing-controls',settings.includes('priority:Number(emailDraft.priority||100)')&&settings.includes('isPrimary:emailDraft.isPrimary'),'UI must submit routing controls');
assert('runbook-gmail',docs.includes('465')&&docs.includes('NOTIFICATION_CREDENTIAL_ENCRYPTION_KEY'),'runbook must document Gmail SMTP and encryption prerequisites');
const migrations=fs.readdirSync(path.join(root,'supabase/migrations')).filter(n=>n.includes('notification')).sort();
const versions=migrations.map(n=>n.match(/^(\\d+)_/)?.[1]).filter(Boolean);
const duplicates=versions.filter((v,i,a)=>a.indexOf(v)!==i);
assert('notification-migration-versions-unique',duplicates.length===0,duplicates.join(','));
console.log('PASS notification-migration-inventory ('+migrations.length+' files)');
console.log('Notification Phase 10 offline contract checks passed.');